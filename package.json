{
  "name": "puppeteer-vercel",
  "version": "1.0.0",
  "description": "Generate image from HTML using Puppeteer in Vercel with chrome-aws-lambda",
  "main": "api/generate-image.js",
  "scripts": {
    "start": "node api/generate-image.js"
  },
  "dependencies": {
    "chrome-aws-lambda": "^10.1.0",
    "puppeteer-core": "^19.11.1"
  },
  "engines": {
    "node": ">=16"
  }
}
